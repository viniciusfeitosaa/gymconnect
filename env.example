# Configurações do Servidor
PORT=5000

# Chave secreta para JWT (IMPORTANTE: Altere em produção!)
JWT_SECRET=gymconnect-secret-key-2024

# Configurações do Banco de Dados (SQLite por padrão)
DB_TYPE=sqlite
DB_PATH=./gymconnect.db

# Configurações de CORS (opcional)
CORS_ORIGIN=http://localhost:3000
